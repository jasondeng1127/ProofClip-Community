// Replace with the HTTPS origin of the Worker you deploy before loading the extension.
// This value is intentionally a non-routable placeholder, never an Official endpoint.
export const COMMUNITY_API_ORIGIN = 'https://replace-me.invalid';
